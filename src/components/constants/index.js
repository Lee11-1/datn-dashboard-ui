import DEFAULT_PLACE from './default_place'

const LIST_EXTENSION_FILE = ['jpg','png','mp4','mp3','txt','csv']

const S3_IMAGE_LINK = 'https://dkq4hg8r4rfz0.cloudfront.net/cms/vnzamazing-tour/images/'

const APP_NAME = 'vnzamazing-tour'

const MAX_PAYLOAD_REQUEST_SIZE = 15 * 1024 * 1024

const ROLE_CODES = {
  admin: 'admin'
}

export {
  DEFAULT_PLACE,
  LIST_EXTENSION_FILE,
  S3_IMAGE_LINK,
  APP_NAME,
  ROLE_CODES,
  MAX_PAYLOAD_REQUEST_SIZE
}
